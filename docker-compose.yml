version: '3'

volumes:
    data:

services:
    mongo:
        image: "mongo:4.0.6"
        volumes:
            - data:/data/db
        restart: unless-stopped
    chibi:
        build: .
        ports:
            - 5000:80
        depends_on:
            - mongo
        restart: unless-stopped
